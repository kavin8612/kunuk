// TODO(task-1.3): primi componenti condivisi e design token — placeholder del bootstrap.
// Esiste solo per dare a tsc ed eslint un sorgente TypeScript valido finché non
// arriva il codice reale del frontend (vedi alberatura: frontend/shared-ui).
export const KUNUK_FRONTEND_BOOTSTRAP = true;
