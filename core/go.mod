module kunuk.dev/core

go 1.23

// TODO(task-0.8): schema DB + moduli backend — placeholder del bootstrap (task 0.1).
// Le dipendenze si aggiungono ai task 0.8–0.9; ogni libreria va verificata all'ultima
// versione e bloccata con versione esatta (doc 19 §5).
